const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  // code to fetch existing blog posts and render the homepage template
});

module.exports = router;